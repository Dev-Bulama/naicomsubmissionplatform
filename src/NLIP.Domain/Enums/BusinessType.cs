namespace NLIP.Domain.Enums;

/// <summary>The only two NAICOM Life Assurance classes this platform integrates.
/// Intentionally does not include Motor/Fire/Marine/etc. per project scope.</summary>
public enum BusinessType
{
    IndividualLife = 1,
    GroupLife = 2
}
